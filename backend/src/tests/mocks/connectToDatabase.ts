const mongoose = require('mongoose');

const connectToDatabase = async () => {
  console.log('Database connected "Mock"'); // This is just a mock, you can adjust it as needed
};

export default connectToDatabase;
